pub enum Pipe0Register {}
pub enum Pipe1Register {}
pub enum Pipe2Register {}
pub enum Pipe3Register {}
pub enum Pipe4Register {}
pub enum Pipe5Register {}

pub enum Pipe {
    Pipe0,
    Pipe1,
    Pipe2,
    Pipe3,
    Pipe4,
    Pipe5,
}
